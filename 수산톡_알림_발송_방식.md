# 수산톡 알림 발송 방식 정리

> 작성일: 2026-06-12  
> 대상 시스템: NCR(부적합 보고서) API 서버  
> 관련 파일: `artifacts/api-server/src/lib/sushantalk.ts`, `notifications.ts`, `sla-scheduler.ts`

---

## 1. 발송 방식 두 가지

수산톡 알림은 **두 가지 방식**을 병행합니다.

| 방식 | 트리거 조건 | 발송 대상 |
|------|------------|-----------|
| **① Webhook** | 항상 사용 가능 (URL만 있으면) | 채널 전체, 부서 그룹 채널 |
| **② PAT API** | `SUSHANTALK_PAT_TOKEN` 환경변수 설정 시 | 개인 DM (이메일 기반) |

---

## 2. 방식 ① — Webhook (채널 발송)

### 작동 원리

```
[서버] ─── POST ──→ [수산톡 Webhook URL]
              body: { "text": "메시지 내용" }
```

수산톡 Webhook URL로 `POST` 요청을 보내면 해당 채널에 메시지가 게시됩니다.

### 사용하는 채널 (환경변수로 설정)

| 환경변수 | 용도 |
|----------|------|
| `SSUSHAN_TALK_QC_WEBHOOK_URL` | QC 담당자 채널 (일반 부적합) |
| `SSUSHAN_TALK_LAB_WEBHOOK_URL` | 개발(Lab) 담당자 채널 (`productType == "개발"` 일 때) |

### 발송 시점 (메인 채널)

메인 QC/Lab 채널에 발송되는 상태는 세 가지뿐입니다.

| 상태 | 의미 | 채널 |
|------|------|------|
| `OPEN` | 부적합 신규 접수 | QC 또는 Lab |
| `APPROVED` | QC 승인 완료 | QC 또는 Lab |
| `ERP_SYNCED` | ERP 등록 완료 | QC 또는 Lab |

### 부서 Webhook (귀책부서 채널)

각 부서는 별도 Webhook URL을 가질 수 있습니다 (`departments.webhook_url` DB 컬럼).  
부서 채널로 발송되는 시점:

| 상태 | To / CC | 의미 |
|------|---------|------|
| `IN_REVIEW` | **To** | QC 검토 시작 → 귀책부서에 통보 |
| `APPROVED` | CC | 승인 완료 참조 |
| `ERP_SYNCED` | **To** | ERP 등록 완료 → 귀책부서에 통보 |

---

## 3. 방식 ② — PAT API (개인 DM)

### 설정 환경변수

| 환경변수 | 내용 |
|----------|------|
| `SUSHANTALK_BASE_URL` | 수산톡 서버 주소 (예: `https://talk.soosan.co.kr`) |
| `SUSHANTALK_PAT_TOKEN` | 개인 액세스 토큰 (Bearer 인증) |

두 값이 모두 설정된 경우에만 PAT 모드가 활성화됩니다.

### 작동 원리

```
[서버] ─── POST ──→ {SUSHANTALK_BASE_URL}/api/external/v1/messages
     Authorization: Bearer {SUSHANTALK_PAT_TOKEN}
     body: {
       "toEmail": "user@soosan.co.kr",   ← 수신자 이메일
       "content": "메시지 내용",
       "botName": "부적합 보고 시스템",
       "roomName": "🔔 NCR 알림"
     }
```

### 수신자 식별 방법

수신자는 **이메일**로 지정합니다. 사용자 테이블(`users.email`)에 이메일이 등록된 경우에만 개인 DM이 발송됩니다.

- 이메일 있음 → PAT API로 개인 DM 발송
- 이메일 없음 → 소속 부서 Webhook으로 Fallback 발송

### 단건 vs 다수 발송

| 함수 | 특징 |
|------|------|
| `sendDm()` | 수신자 1명 |
| `sendBulkDm()` | 수신자 여러 명 동시 발송 (최대 100명), 성공/실패 수 반환 |

---

## 4. 상태별 수신자 라우팅

보고서 상태가 바뀔 때 **역할(role) 기반**으로 수신자를 결정합니다.

| 상태 | To 수신자 (상세 메시지) | CC 수신자 (참조 메시지) |
|------|------------------------|------------------------|
| `OPEN` (접수) | reviewer, approver | admin |
| `IN_REVIEW` (검토 중) | _(귀책부서 webhook)_ | reviewer, approver |
| `PENDING_COLLAB` (협업 요청) | 댓글에서 @태그된 사용자 | — |
| `RESOLVED` (조치 완료) | approver | reviewer, admin |
| `APPROVED` (승인) | admin | reviewer, approver |
| `ERP_SYNCED` (ERP 등록) | reviewer, approver | — |

### notifyLevel (사용자별 수신 레벨)

각 사용자의 `notify_level` 컬럼 값에 따라 수신 여부가 결정됩니다.

| 값 | 의미 |
|----|------|
| `to` | To 대상일 때 상세 메시지, CC 대상일 때 참조 메시지 모두 수신 |
| `cc` | CC 참조 메시지만 수신 (To 대상이어도 요약본 수신) |
| `none` | 알림 차단 (수신 안 함) |

---

## 5. SLA 초과 알림

보고서 접수 후 **24시간 내 QC 조치가 없으면** 자동으로 잠금 처리되며, 동시에 알림이 발송됩니다.

### 스케줄러

- 주기: **5분마다** 만료 보고서 검사
- 조건: `sla_deadline_at < now()` AND `is_locked = false` AND `qc_action IS NULL`

### SLA 알림 발송 대상

| 대상 | 방식 |
|------|------|
| QC 메인 채널 (qc 또는 lab) | Webhook |
| 귀책부서 채널 | Webhook (설정된 경우) |
| reviewer / approver / admin | PAT 개인 DM (이메일 등록 + PAT 설정된 경우) |

### SLA 알림 메시지 형식

```
[⏰ SLA 초과 잠금] NCR-2026-001
품목: 1129114
공정: 1라인
등록자: 홍길동
접수 후 24시간이 경과하여 보고서가 잠금 처리되었습니다.
링크: https://code-transformer.replit.app/ledger?reportId=8
```

---

## 6. 메시지 형식

### To 메시지 (상세)

```
[부적합 접수] NCR-2026-001
품목: 1129114
공정: 1라인
등록자: 홍길동
링크: https://code-transformer.replit.app/ledger?reportId=9
```

### CC 메시지 (참조)

```
[참조] NCR-2026-001 상태 변경: 신규 → 부적합 접수
품목: 1129114
링크: https://code-transformer.replit.app/ledger?reportId=9
```

---

## 7. 전체 흐름 요약

```
보고서 상태 변경 (또는 신규 접수)
        │
        ▼
notifyStatusTransition() 호출
        │
        ├─ PAT 설정됨?
        │       ├─ Yes → 이메일 있는 사용자 → sendBulkDm() → 개인 DM
        │       │         이메일 없는 사용자 → 부서 Webhook fallback
        │       └─ No  → 부서 Webhook 으로만 발송
        │
        ├─ 귀책부서 Webhook 설정됨? → IN_REVIEW / APPROVED / ERP_SYNCED 시 발송
        │
        └─ OPEN / APPROVED / ERP_SYNCED → QC 또는 Lab 메인 채널 Webhook 발송

SLA 5분 주기 체크
        │
        └─ 24시간 초과 보고서 발견
                │
                ├─ is_locked = true 처리
                └─ notifySlaLocked() → 메인 채널 + 부서 Webhook + PAT DM
```

---

## 8. 환경변수 목록

| 변수명 | 필수 여부 | 설명 |
|--------|-----------|------|
| `SSUSHAN_TALK_QC_WEBHOOK_URL` | 권장 | QC 채널 Webhook URL |
| `SSUSHAN_TALK_LAB_WEBHOOK_URL` | 선택 | Lab(개발) 채널 Webhook URL |
| `SUSHANTALK_BASE_URL` | PAT 사용 시 필수 | 수산톡 API 서버 주소 |
| `SUSHANTALK_PAT_TOKEN` | PAT 사용 시 필수 | 개인 액세스 토큰 |
| `APP_URL` | 권장 | 알림 링크에 쓰일 앱 주소 (기본값: `https://your-app.replit.app`) |
