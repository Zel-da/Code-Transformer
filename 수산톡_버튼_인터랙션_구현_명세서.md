# 수산톡 버튼 인터랙션 구현 명세서

> 작성일: 2026-06-12  
> 목적: NCR 시스템 알림 메시지에 인터랙티브 버튼을 추가하여, 수산톡에서 바로 QC 상태 전이를 수행할 수 있도록 한다.

---

## 1. 목표 동작 흐름

```
[NCR 상태 변경]
      │
      ▼
notifyStatusTransition() — actions[] 포함 메시지 발송
      │
      ▼
[수산톡 DM 수신자에게 버튼 표시]
      │
      ▼ 버튼 클릭 (예: ✅ 승인)
      │
      ▼
수산톡 → POST {NCR 서버}/api/webhooks/ncr-action (callbackUrl)
      │
      ▼
NCR API: 보고서 QC 상태 전이 수행 (RESOLVED → APPROVED 등)
      │
      ▼
[상태 변경 완료 + 다음 수신자에게 후속 알림 발송]
```

---

## 2. 상태별 버튼 설계

보고서 상태가 바뀔 때 **To 수신자** 메시지에만 버튼을 포함한다. CC 메시지에는 버튼 없음(참조용).

### 2-A. OPEN (부적합 접수) — To: reviewer, approver

| 버튼 | label | value | style | type |
|------|-------|-------|-------|------|
| 검토 시작 | `🔍 검토 시작` | `IN_REVIEW` | `primary` | `postback` |
| 보고서 보기 | `📋 보고서 보기` | `{앱URL}/ledger?reportId={id}` | `default` | `url` |

### 2-B. RESOLVED (조치 완료) — To: approver

| 버튼 | label | value | style | type |
|------|-------|-------|-------|------|
| 승인 | `✅ 승인` | `APPROVED` | `primary` | `postback` |
| 반려 | `↩️ 반려` | `IN_REVIEW` | `danger` | `postback` |
| 보고서 보기 | `📋 보고서 보기` | `{앱URL}/ledger?reportId={id}` | `default` | `url` |

### 2-C. APPROVED (승인 완료) — To: admin

| 버튼 | label | value | style | type |
|------|-------|-------|-------|------|
| ERP 등록 완료 | `🖥️ ERP 등록 완료` | `ERP_SYNCED` | `primary` | `postback` |
| 보고서 보기 | `📋 보고서 보기` | `{앱URL}/ledger?reportId={id}` | `default` | `url` |

### 2-D. PENDING_COLLAB (협업 요청) — To: @태그된 사용자

| 버튼 | label | value | style | type |
|------|-------|-------|-------|------|
| 확인 완료 | `✅ 확인 완료` | `IN_REVIEW` | `primary` | `postback` |
| 보고서 보기 | `📋 보고서 보기` | `{앱URL}/ledger?reportId={id}` | `default` | `url` |

### 2-E. 버튼 없는 상태 (현행 유지)

| 상태 | 이유 |
|------|------|
| `IN_REVIEW` | 귀책부서 webhook(채널)으로만 발송, 개인 DM 없음 |
| `ERP_SYNCED` | 최종 완료 상태, 후속 액션 없음 |
| SLA 초과 잠금 | 알림 전용, 직접 전이 불필요 |

---

## 3. callbackUrl 설계

### 엔드포인트

```
POST https://code-transformer.replit.app/api/webhooks/ncr-action
```

수산톡은 버튼 클릭 시 이 URL로 POST를 보낸다. **10초 이내에 2xx 응답** 필요.

### 수신 페이로드 (수산톡 → NCR)

```json
{
  "messageId": 1234,
  "userId": 101,
  "actionId": "attend",
  "value": "APPROVED",
  "name": "홍길동",
  "email": "gildong.hong@soosan.co.kr",
  "triggeredAt": "2026-06-12T14:23:11.000Z"
}
```

### value 인코딩 방식

`postback` 버튼의 `value` 필드에 상태 전이 정보를 JSON으로 인코딩한다.

```
value = "{targetStatus}:{reportId}:{hmacSignature}"
```

예시:
```
"value": "APPROVED:42:a1b2c3d4e5"
```

- `targetStatus`: 전이할 QC 상태 (`IN_REVIEW`, `APPROVED`, `ERP_SYNCED` 등)
- `reportId`: 보고서 ID
- `hmacSignature`: `WEBHOOK_SECRET`으로 서명한 HMAC-SHA256 앞 10자리 (위변조 방지)

### 처리 로직 (의사코드)

```typescript
POST /api/webhooks/ncr-action

1. res.sendStatus(200)  // 즉시 응답 (10초 타임아웃 방지)

2. 비동기 처리:
   a. value 파싱: "APPROVED:42:a1b2c3d4"
   b. HMAC 서명 검증 → 실패 시 무시
   c. 보고서 #42 조회
   d. 클릭한 사람 이메일로 NCR 사용자 조회 (users.email = email)
   e. 해당 사용자의 role이 전이 권한이 있는지 TRANSITION_MATRIX 확인
   f. 전이 수행 + 감사 로그 기록
   g. 후속 알림 발송 (notifyStatusTransition)
```

### 실패 처리

| 상황 | 처리 |
|------|------|
| HMAC 서명 불일치 | 무시 (로그만 기록) |
| 사용자 이메일이 NCR에 없음 | 무시 |
| 권한 없음 (role 불일치) | 무시 |
| 이미 다른 상태 (전이 불가) | 무시 |
| 버튼 중복 클릭 | 수산톡에서 409로 차단 (NCR 도달 안 함) |

---

## 4. 보안

### 서명 검증 (HMAC-SHA256)

callbackUrl은 공개 URL이므로 위조 요청 방지가 필요하다.

**서명 생성 (메시지 발송 시)**:
```typescript
const payload = `${reportId}:${targetStatus}`;
const sig = hmacSha256(payload, process.env.NCR_WEBHOOK_SECRET).slice(0, 10);
const value = `${targetStatus}:${reportId}:${sig}`;
```

**서명 검증 (콜백 수신 시)**:
```typescript
const [targetStatus, reportId, sig] = value.split(":");
const expected = hmacSha256(`${reportId}:${targetStatus}`, process.env.NCR_WEBHOOK_SECRET).slice(0, 10);
if (sig !== expected) return; // 무시
```

### 추가 조치
- `NCR_WEBHOOK_SECRET` 환경변수로 관리 (Replit Secrets)
- 콜백 처리는 항상 200 즉시 응답 후 비동기 실행 (타임아웃 방지)
- 수산톡 측 버튼 1회 클릭 제한으로 중복 전이 자동 방지

---

## 5. 변경이 필요한 파일

### 5-A. `artifacts/api-server/src/lib/sushantalk.ts`

`MessageAction` 타입 추가 및 `SendDmOptions` / `SendBulkOptions`에 `actions?` 필드 추가.

```typescript
// 추가할 타입
export interface MessageAction {
  id: string;
  label: string;
  value: string;
  style?: "default" | "primary" | "danger";
  type?: "postback" | "url";
  callbackUrl?: string;
}

// 기존 옵션 인터페이스에 추가
export interface SendDmOptions {
  // 기존 필드들...
  actions?: MessageAction[];   // ← 추가
}

export interface SendBulkOptions {
  // 기존 필드들...
  actions?: MessageAction[];   // ← 추가
}
```

`sendDm` / `sendBulkDm` 함수 body에 `actions` 포함:
```typescript
const body = {
  ...opts.recipient,
  content: opts.content,
  ...(opts.actions ? { actions: opts.actions } : {}),  // ← 추가
};
```

---

### 5-B. `artifacts/api-server/src/lib/notifications.ts`

`buildToMessage` 함수 → `buildToActions` 함수 추가.  
`notifyStatusTransition` 내 DM 발송 시 `actions` 포함.

```typescript
// 추가할 함수
function buildToActions(
  report: NonConformityReport,
  to: QcStatus,
  appUrl: string,
  webhookSecret: string,
): MessageAction[] {
  const reportId = report.id;
  const link = `${appUrl}/ledger?reportId=${reportId}`;

  function makeSig(targetStatus: string) {
    // HMAC-SHA256 앞 10자리
    return hmac(`${reportId}:${targetStatus}`, webhookSecret).slice(0, 10);
  }

  function postback(targetStatus: QcStatus, label: string, style: MessageAction["style"]): MessageAction {
    const sig = makeSig(targetStatus);
    return {
      id: targetStatus.toLowerCase(),
      label,
      value: `${targetStatus}:${reportId}:${sig}`,
      style,
      type: "postback",
      callbackUrl: `${appUrl.replace("/ncr-app", "")}/api/webhooks/ncr-action`,
    };
  }

  const viewBtn: MessageAction = {
    id: "view",
    label: "📋 보고서 보기",
    value: link,
    style: "default",
    type: "url",
  };

  switch (to) {
    case "OPEN":
      return [postback("IN_REVIEW", "🔍 검토 시작", "primary"), viewBtn];
    case "RESOLVED":
      return [
        postback("APPROVED", "✅ 승인", "primary"),
        postback("IN_REVIEW", "↩️ 반려", "danger"),
        viewBtn,
      ];
    case "APPROVED":
      return [postback("ERP_SYNCED", "🖥️ ERP 등록 완료", "primary"), viewBtn];
    case "PENDING_COLLAB":
      return [postback("IN_REVIEW", "✅ 확인 완료", "primary"), viewBtn];
    default:
      return [viewBtn];
  }
}
```

DM 발송 시 actions 포함:
```typescript
// sendBulkDm 호출 시
sendBulkDm({
  recipients: dmTo,
  content: toMsg,
  botName: BOT_NAME,
  roomName: ROOM_NAME,
  actions: buildToActions(report, to, appUrl, webhookSecret),  // ← 추가
})
```

---

### 5-C. `artifacts/api-server/src/routes/webhooks.ts` (신규 파일)

```typescript
// POST /api/webhooks/ncr-action
// 수산톡 버튼 클릭 콜백 수신 및 QC 상태 전이 처리

router.post("/webhooks/ncr-action", async (req, res) => {
  // 즉시 200 응답 (수산톡 10초 타임아웃 방지)
  res.sendStatus(200);

  // 비동기 처리
  setImmediate(async () => {
    const { value, email, name, triggeredAt } = req.body;

    // 1. value 파싱: "APPROVED:42:a1b2c3d4"
    const parts = (value ?? "").split(":");
    if (parts.length !== 3) return;
    const [targetStatus, reportIdStr, sig] = parts;
    const reportId = Number(reportIdStr);
    if (isNaN(reportId)) return;

    // 2. HMAC 서명 검증
    const secret = process.env.NCR_WEBHOOK_SECRET ?? "";
    const expected = hmac(`${reportId}:${targetStatus}`, secret).slice(0, 10);
    if (sig !== expected) {
      logger.warn({ reportId, targetStatus }, "ncr-action: invalid signature");
      return;
    }

    // 3. 클릭한 사용자 조회 (이메일 기준)
    const [actor] = await db.select().from(usersTable)
      .where(eq(usersTable.email, email));
    if (!actor) {
      logger.warn({ email }, "ncr-action: user not found");
      return;
    }

    // 4. 전이 권한 확인 (TRANSITION_MATRIX)
    const [report] = await db.select().from(nonConformityReportsTable)
      .where(eq(nonConformityReportsTable.id, reportId));
    if (!report) return;

    const from = report.qcStatus as QcStatus;
    const allowed = TRANSITION_MATRIX[from]?.[targetStatus as QcStatus];
    if (!allowed?.includes(actor.role as UserRole)) {
      logger.warn({ from, targetStatus, role: actor.role }, "ncr-action: unauthorized");
      return;
    }

    // 5. 상태 전이
    await db.update(nonConformityReportsTable)
      .set({ qcStatus: targetStatus, qcSubmittedAt: new Date(), qcSubmittedBy: actor.id })
      .where(eq(nonConformityReportsTable.id, reportId));

    // 6. 감사 로그
    await writeAuditLog({
      actorId: actor.id,
      actorName: name,
      action: "STATUS_CHANGED",
      targetType: "report",
      targetId: reportId,
      before: { qcStatus: from },
      after: { qcStatus: targetStatus },
      detail: `수산톡 버튼 클릭: ${from} → ${targetStatus}`,
    });

    // 7. 후속 알림 발송
    await notifyStatusTransition({ report: { ...report, qcStatus: targetStatus }, from, to: targetStatus });

    logger.info({ reportId, from, targetStatus, actor: email }, "ncr-action: status transitioned via Sushantalk button");
  });
});
```

---

### 5-D. `artifacts/api-server/src/routes/index.ts`

webhooks 라우터 등록:
```typescript
import webhooksRouter from "./webhooks.js";
app.use("/api", webhooksRouter);
```

---

### 5-E. 환경변수 추가

| 변수명 | 설명 | 예시 |
|--------|------|------|
| `NCR_WEBHOOK_SECRET` | HMAC 서명 키 (임의 랜덤 문자열) | `openssl rand -hex 32` 로 생성 |

---

## 6. 구현 범위 요약

| 항목 | 파일 | 작업량 |
|------|------|--------|
| MessageAction 타입 + actions 파라미터 | `sushantalk.ts` | 소 |
| 버튼 생성 함수 + DM 발송에 actions 포함 | `notifications.ts` | 중 |
| 콜백 수신 웹훅 엔드포인트 | `webhooks.ts` (신규) | 중 |
| 라우터 등록 | `routes/index.ts` | 소 |
| 환경변수 설정 | Replit Secrets | 소 |

---

## 7. 구현 후 동작 예시

수산톡 메시지 수신 화면:
```
┌─────────────────────────────────┐
│ 🤖 부적합 보고 시스템            │
│                                 │
│ [조치 완료] NCR-2026-042         │
│ 품목: 1129114                   │
│ 공정: 1라인                     │
│ 등록자: 홍길동                   │
│ 링크: https://...               │
│                                 │
│  ✅ 승인  ↩️ 반려  📋 보고서 보기│
└─────────────────────────────────┘
```

버튼 클릭 시:
1. 수산톡 → NCR 서버에 콜백
2. NCR에서 APPROVED 상태 전이
3. admin(RPA 담당자)에게 후속 알림 발송

---

## 8. 제약 사항

- 버튼은 최대 5개 (수산톡 API 제한)
- 사용자 1인당 1회만 응답 가능 (수산톡 자체 중복 방지)
- callbackUrl은 **공개 HTTPS**여야 함 (프로덕션 URL 사용)
- 개발환경(localhost)에서는 콜백 수신 불가 → ngrok 등 터널링 필요
- 이메일이 등록된 사용자에게만 버튼 포함 DM 발송 (이메일 없으면 webhook fallback, 버튼 없음)
